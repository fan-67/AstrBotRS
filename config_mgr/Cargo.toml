[package]
name = "astrbot-config-mgr"
version = "0.1.0"
edition = "2024"
description = "Configuration manager for AstrBot RS"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
serde_json = "1"
thiserror = "2"
astrbot-utils = { path = "../utils" }
